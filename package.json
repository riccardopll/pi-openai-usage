{
  "name": "@riccardopll/pi-openai-usage",
  "description": "Pi extension that shows OpenAI weekly usage.",
  "keywords": [
    "codex",
    "openai",
    "pi-extension",
    "pi-package",
    "usage"
  ],
  "homepage": "https://github.com/riccardopll/pi-openai-usage#readme",
  "bugs": "https://github.com/riccardopll/pi-openai-usage/issues",
  "license": "MIT",
  "author": "Riccardo Palleschi",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/riccardopll/pi-openai-usage.git"
  },
  "files": [
    "extensions"
  ],
  "type": "module",
  "scripts": {
    "check": "oxfmt --check . && oxlint . && tsc --noEmit",
    "fix": "oxfmt --write . && oxlint --fix ."
  },
  "devDependencies": {
    "@earendil-works/pi-coding-agent": "^0.82.0",
    "@earendil-works/pi-tui": "^0.82.0",
    "oxfmt": "^0.59.0",
    "oxlint": "^1.74.0",
    "typescript": "^7.0.2"
  },
  "peerDependencies": {
    "@earendil-works/pi-coding-agent": "*",
    "@earendil-works/pi-tui": "*"
  },
  "engines": {
    "node": ">=22.19.0"
  },
  "pi": {
    "extensions": [
      "./extensions/openai-usage.ts"
    ]
  }
}
